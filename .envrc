# Auto-load the flake devShell (compiler, dune, opam, python) on cd into the
# repo. Uses nix-direnv's `use flake`, which caches the dev environment so it is
# only rebuilt when flake.nix / flake.lock change — the same toolchain as
# `nix develop`. Run `direnv allow` once to enable.
use flake
